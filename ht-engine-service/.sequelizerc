const path = require("path");

module.exports = {
  "config": path.resolve("config", "datastore.js"),
  "models-path": path.resolve("dist", "models", "entities"),
  "seeders-path": path.resolve("seeders"),
  "migrations-path": path.resolve("migrations")
}
